# HANDOFF — 2026-08-13

> 给下一个会话（或接手者）的交接说明。当前仓库已对齐 GitHub 最新版并完成 S1 架构债修复的第一批。

## 一句话状态

《升维 · Ascend》已对齐 GitHub `piprot/shengwei-game-starter` **v1.7.36**，完成完整性评估 + S1 四项中的三项（乱码 / 能力模型 / 主题回退）+ 命名清理，并完成 **App.ts 拆分的阶段 1 + 2**（纯函数提取）。剩余 App.ts 拆分的 5 个组合模式域待后续会话逐一推进。

## 当前 git 状态

工作区干净，7 个提交（每个独立可回滚）：

| 提交 | 内容 |
|---|---|
| `cc684dd` | git 基线（对齐上游 v1.7.36，忽略本机 `CLAUDE.md`/`.claude/`）|
| `089a85b` | 修复 14 处 GBK mojibake 乱码注释 |
| `4549138` | 统一能力模型命名（五维→领导力画像、四维→学院影响力）|
| `4e3fe73` | 回退藏宝图主题（文明/残片→九章权力架构、探秘→勘察）|
| `a63e965` | 清理命名残留 + 视觉 prompt（文物残片→领导力隐喻）|
| `2f4e10d` | App.ts 拆分阶段 1：提取 display 7 纯函数 |
| `1654b94` | App.ts 拆分阶段 2：提取导出域 export.ts + display 扩 3 函数 |

## 关键决策（勿在后续会话推翻）

1. **能力模型**：以「十项能力谱系」（`abilities.ts`）为唯一主能力；「领导力五维」已改名**「领导力画像」**（十项能力的聚合视图）；「团队学院四维」已改名**「学院影响力」**。
2. **主题**：已回退「藏宝图/四大古文明」探险包装，回归「九章权力架构 + 职场情境」严肃调性；「探秘」统一改「勘察」。
3. **App.ts 拆分粒度**：按「域」而非按「24 个视图各拆一类」。
4. **刻意保留的兼容标识符**（勿改名，会破坏存档/测试）：
   - `explorationFound` / `explorationCompleted`（存档字段，localStorage 持久化）
   - `expedition-explore`（`data-action`，`scripts/smoke.mjs` 依赖）
   - `treasure-fragment-*.jpg` 已改为 `power-stage-*.jpg`（图片文件已重命名，但 `generate-real-art.mjs` 里 name/prompt 已同步）

## App.ts 拆分进度

- ✅ **阶段 1**：`src/ui/display.ts`（`rankName/chapterDisplay/abilityDisplay/abilityDetailDisplay/roleDisplay/resourceDisplay/qualityLabel` + `npcDisplay/relationStatusText/sideArcDisplay`）— 显式接收 `language` 参数的纯函数。`t()`（325 处调用）保留在 App 内。
- ✅ **阶段 2**：`src/ui/export.ts`（`downloadText/encodeSaveLink/buildReportMarkdown`），导出方法在 App 内变薄壳。
- ⏳ **剩余 5 域**（组合模式，见下）：

| 顺序 | 域 | 规模（约） | 备注 |
|---|---|---|---|
| 3 | 教练 coach | ~500 行 | 已有 `CoachWorkshopEngine` 独立引擎，边界最清晰，**下次从这里开始** |
| 4 | 试炼 trial | ~400 行 | 已有 `trials.ts` 引擎 + `trial*` 独立状态字段 |
| 5 | 云端 cloud | ~300 行 | 已有 `RoomClient` 独立类；跨 `renderReport`/`renderDuelLobby` + duel 域交叉；静态版不执行 |
| 6 | 1v1 | ~2000 行 | 最大单块，~40 方法 + ~30 字段；拆完跑 `test:rtc` |
| 7 | 核心循环 | ~3000 行 | map/story/profile/assessment/report/ability/ending 等；建议只拆 report+ability，map/story 保留 |

App.ts 当前约 **10000 行**（从 10300 起）。

## 拆分模式（沿用，已验证）

项目里 `TeamAcademyApp` / `LeadershipGamesApp` 是组合模式样板：

```ts
export interface TeamAcademyCallbacks {
  onBack(): void;
  onAudio(kind: "ui" | "correct" | "wrong"): void;
}
export class TeamAcademyApp {
  // 持有视图专属状态；共享能力（save/language/audio/show）通过 callbacks 注入
  render(root: HTMLElement): void;
  handleAction(action: string, target: HTMLElement): void;
}
// App 持有实例，render 委托 + handleClick 按 action 前缀（ta-/lg-）路由
```

新域拆分照此模式：`XxxController` 类 + callbacks（`getSave`/`onAudio`/`onBack`/`onNavigate` 等）。

## 下一步：教练域拆分（下次会话入口）

1. 读 `src/ui/App.ts` 的 `renderCoach` / `loadCoachDemo` / `importCoachParticipants` / `coachDemoParticipants` / `coachPlanMarkup` / `liveMarkup` + `coach*` 状态字段。
2. 新建 `src/ui/coach-view.ts`，仿 `TeamAcademyApp`：`CoachView` 类持有 `coach*` 状态，callbacks 注入 `getSave`/`onAudio`/`onBack`/`setCoachReport` 等。
3. `handleClick` 里 coach 相关 case 改为委托（`view==="coach"` 前缀路由）。
4. 验证 `npm run build` + `npm test`（smoke 已覆盖教练工作坊），全绿后 `git commit`。

## 重要文件

- 待拆：`src/ui/App.ts`（约 10000 行）
- 已拆出：`src/ui/display.ts`、`src/ui/export.ts`
- 样板：`src/ui/team-academy.ts`、`src/ui/leadership-games.ts`
- 计划文件：`C:\Users\Administrator\.claude\plans\memoized-plotting-sedgewick.md`
- 评估报告：`docs/EVALUATION_2026-08-13.md`
- 项目文档：`CODEX_HANDOFF.md`（旧，历史）、`PRD.md`、`PROJECT_PLAN.md`

## 验证命令

```bash
npm run build          # tsc + vite（每个阶段必跑）
npm test               # smoke 端到端（建档→主线→勘察→AI 1v1→本地双人→教练工作坊）
npm run audit          # 桌面/手机布局 + 控制台错误
npm run test:system    # 13 视图可导航
npm run test:rtc       # 远程对战（拆 1v1 域后必跑）
npm run test:server    # 云端（拆 cloud 域后必跑）
```

## 注意事项

1. **文件是 CRLF 换行**：用 Node 脚本批量改文本时，先 `src.replace(/\r\n/g, "\n")` 归一化处理，写回前还原，否则字符串匹配会失败（阶段 1 踩过这个坑）。
2. **dev server 监听 IPv6 `::1`**：本地访问用 `http://localhost:5173/`（不是 `127.0.0.1`）；重启用 `npm run dev -- --host`。
3. **组合模式拆分后**：每个域独立 commit，全绿再进下一个；行号会因前序删除而偏移，用「精确文本匹配」而非「行号」做 Edit。
4. **本机环境**：claude 走 cc-switch（`127.0.0.1:15721`）→ DeepSeek，无需 Anthropic key；`CLAUDE.md` 与 `.claude/` 已 gitignore。
